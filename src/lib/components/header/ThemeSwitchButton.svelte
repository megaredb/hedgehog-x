<script lang="ts">
	import { toggleMode, mode } from 'mode-watcher';
	import { Moon, Sun } from '@lucide/svelte';
	import { Button } from '$lib/components/ui/button';
</script>

<Button variant="outline" size="icon" onclick={toggleMode} aria-label="Переключить тему">
	{#if mode.current === 'dark'}
		<Sun class="size-5" />
	{:else}
		<Moon class="size-5" />
	{/if}
</Button>
